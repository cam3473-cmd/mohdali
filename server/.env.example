DATABASE_URL="file:./dev.db"
PORT=4000

# JWT_SECRET اختياري: إن تُرك فارغاً أو على قيمته الافتراضية غير الآمنة أدناه، يقوم الخادم
# تلقائياً بتوليد مفتاح عشوائي عند أول تشغيل وحفظه في ملف .jwt-secret بجانب قاعدة البيانات.
# لضبط قيمة ثابتة يدوياً (مثلاً في بيئة يُعاد فيها إنشاء القرص بلا حالة دائمة)، عيّنها هنا:
# JWT_SECRET="change-this-secret-in-production"

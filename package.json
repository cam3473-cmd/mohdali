{
  "name": "jamiyat-al-birr-alsulail",
  "private": true,
  "version": "0.1.0",
  "description": "نظام إدارة بيانات المستفيدين والدعوم - جمعية البر الخيرية بمحافظة السليل",
  "workspaces": [
    "server",
    "web",
    "electron"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "build:web": "npm run build --workspace web",
    "build:server": "npm run build --workspace server",
    "build": "npm run build:web && npm run build:server",
    "db:migrate": "npm run db:migrate --workspace server",
    "db:seed": "npm run db:seed --workspace server",
    "electron": "npm run start --workspace electron",
    "dist": "npm run build && npm run dist --workspace electron"
  }
}
